# Texture Trainer icon wiring

- Reused the existing SVG at `assets/icons/modules/texture-trainer.svg`.
- Confirmed the home module card points to that icon.
- Confirmed Texture Trainer uses the icon in the topbar chip, left setup hero, centre listening console and right answer-card empty state.
- Added a preload hint for the module icon on `modules/texture-trainer/index.html`.
- Added the same white/glow treatment used by the EchoAural module-card icons so the SVG remains visible when loaded as an external image.

No Melody Master, Instrument Identifier or Teacher Mode functionality was changed.
